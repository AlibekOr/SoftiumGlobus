import {View} from "react-native";

interface IProps {
    id: number,
    images: any,
    name: string,
    price: number
}

export const CartProduct = (props: IProps) => {
    return (
        <View>

        </View>
    )
}