import {Text, View} from "react-native";

export const SearchInput = ({name}: { name: string }) => {
    console.log(name)
    return (
        <View>
            <Text>search</Text>
        </View>
    )
}